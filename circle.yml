machine:
  node:
    version: 6.0.0
dependencies:
  pre:
    - "npm i -g istanbul"
    - "npm i -g eslint"
test:
  post:
    - npm run coveralls
    - mkdir -p $CIRCLE_ARTIFACTS/
    - mv ./coverage/lcov.info  $CIRCLE_ARTIFACTS/coverage-report
