machine:
  node:
    version: 6.0.0
dependencies:
  pre:
    - "npm i -g istanbul"
    - "npm i -g eslint"
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/
    - mv ./coverage/coverage.json  $CIRCLE_ARTIFACTS/coverage-report
